-- Arleco staff picks — prefer the in-site admin UI after supabase-admin-featured.sql
--
-- Setup (once):
--   1. Run docs/supabase-admin-featured.sql
--   2. Uncomment and run the bootstrap block there to set is_admin on your account
--   3. Log in → header link "Staff picks" → /admin/featured
--
-- Manual SQL below is only needed if you cannot use the admin page.

-- See docs/supabase-admin-featured.sql for RPC-based curation (recommended).
